services:
  - type: web
    name: blue-line-breakdown-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --timeout 600 --workers 1 app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"